name: pansy_ui
description: A pretty GUI kit for Flutter.
version: 0.0.1
homepage: https://github.com/oneLab-Projects/picbox

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # design
  ubuntu_font_flutter: ^0.0.1
  transparent_image: ^1.0.0

  # localizations
  devicelocale: ^0.2.2

  # bloc
  rxdart: ^0.23.1
  provider: ^4.0.4

  # db
  shared_preferences: ^0.5.6+2

  # compare
  equatable: ^1.1.0

dev_dependencies:
  pedantic: ^1.9.0
